# ============================================
# Production Environment Variables Template
# ============================================
# Copy this file to .env.production and fill in the values

# ----------------------------------------
# Database
# ----------------------------------------
POSTGRES_USER=cms_user
POSTGRES_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>
POSTGRES_DB=complaint_system

# ----------------------------------------
# MinIO (S3-compatible storage)
# ----------------------------------------
MINIO_ROOT_USER=minio_admin
MINIO_ROOT_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>
S3_BUCKET=complaint-photos
S3_REGION=us-east-1
# Set this to your production MinIO/S3 endpoint accessible from outside Docker
S3_ENDPOINT_PUBLIC=https://storage.yourdomain.com

# ----------------------------------------
# Backend Security
# ----------------------------------------
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=<CHANGE_ME_GENERATE_WITH_SECRETS>
BACKEND_SERVICE_TOKEN=<CHANGE_ME_GENERATE_WITH_SECRETS>

# ----------------------------------------
# Telegram Bot
# ----------------------------------------
TELEGRAM_BOT_TOKEN=<YOUR_BOT_TOKEN_FROM_BOTFATHER>

# ----------------------------------------
# Ports (optional, defaults shown)
# ----------------------------------------
HTTP_PORT=80

# ----------------------------------------
# Application
# ----------------------------------------
APP_ENV=production
