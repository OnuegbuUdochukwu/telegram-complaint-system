# Database Configuration
# ----------------------
# Connection string for PostgreSQL.
# Format: postgresql+psycopg2://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgresql+psycopg2://cms_user:cms_user_password@localhost:5432/cms_db

# Authentication & Security
# -------------------------
# Secret key for signing JWT tokens. Generate a strong random string (e.g. `openssl rand -hex 32`).
# REQUIRED: The application will fail to start if this is not set.
JWT_SECRET=change-me-to-a-secure-random-string

# Token expiration time in minutes (default: 60)
JWT_ACCESS_MINUTES=60

# Optional: Admin email for initial bootstrap or testing
# AUTO_ADMIN_EMAILS=admin@example.com

# Storage Configuration
# ---------------------
# Provider: 's3' or 'local' (default: s3)
STORAGE_PROVIDER=s3

# S3 Configuration (Required if STORAGE_PROVIDER=s3)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your-bucket-name
# S3_ENDPOINT_URL=https://s3.amazonaws.com  # Optional, for MinIO/LocalStack

# Observability
# -------------
# Sentry DSN for error tracking (optional)
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Telegram Bot Integration (Optional for Backend, Required for Bot)
# ---------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
